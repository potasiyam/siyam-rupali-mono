# PROOF — Siyam Rupali Mono 0.0.8, this box, 2026-09-03

All artifacts in this directory. Font under test: fresh rebuild of the
committed pipeline under a poison-proof family name
**"Siyam Rupali Mono Proof"** (`build/SiyamRupaliMono-Proof.ttf`,
name table `Version 0.0.8`, advance 1404, 396 CV ligatures).

## 1. Reproducibility + gates
- `mono_convert` + `gen_cv_ligatures` from `legacy/base-1.070ship.ttf`:
  396 ligatures, cell 1404 — matches 0.0.8.
- `shape_check --cell 1404 --matrix --context` = **0 failures**.
- Programmatic shaping (vharfbuzz) — full spacing string = **13.00
  cells**, byte-consistent with the committed SPACING_REPORT table's
  "Two (universal 0.0.8)" row (13.00).
- Installed family "Siyam Rupali Mono" file (008) == `build/…-008.ttf`
  by SHA256 — the box's registry points at the real 0.0.8 artifact.

## 2. Which-font verification (Rule 5)
- Fresh-process WPF/DWrite: Proof `ক` = **10.97 px** @ 16 px em =
  1404/2048 exactly; old family also 10.97 (resolves fine in fresh
  processes); Nirmala fallback signature = 14.05 px (not seen).
- Captures show our letterforms AND our shaping: WT renders full
  reorder+GSUB (reph over ত, ্য conjunct, matras positioned, কি merged
  with empty col 2 = our ligature fired). WezTerm shaped. Alacritty
  unshaped (codepoint-order art, dotted circles on ো/ৌ — known).

## 3. Live column charging (cursor probe, capture-verified)
| case | cps | WT | WezTerm | Alacritty |
|---|---|---|---|---|
| ka | 1 | 1 | 1 | 1 |
| kaa | 2 | 2 | 2 | 2 |
| ki | 2 | 2 | 2 | 2 |
| kang | 2 | 2 | 2 | 2 |
| **king** | 3 | **2** | 3 | 3 |
| ko | 3 | 2 | 2 | 2 |
| kou | 3 | 2 | 2 | 2 |
| kssa | 3 | 2 | 3 | 3 |
| korto | 4 | 3 | 4 | 4 |
| kortobbo | 7 | 5 | 7 | 7 |
| bimurho | 6 | 4 | 6 | 6 |
| iuke | 4 | 4 | 4 | 4 |
| full (with korto) | 26 | **20** | 26 | 26 |

- **WT fits the refined model on every row**: Extend (্ ু ়) = 0,
  mark-run collapse (ে+া→1, ি+ং→0), ং = 1 after a non-mark (kang=2).
  full=20 is the EXACT sum of WT's own word values (additive ✓).
- **ং verdict (live, this box, today): WT collapses ং after a mark**
  (king=2), agreeing with the other box's bisect. "ং should be 1"
  therefore describes the reference model (font truth: anusvara carries
  a full advance — implemented in `tools/spacing_report.py`), while
  WT's charging undercharges it. Residual is terminal-side.
- WT ki=2 while shaped ki=1 → the charged-but-empty 2nd column
  (4th-revision model) — font-independent, present with ligatures.
- **WezTerm/Alacritty on Windows charge raw codepoints** (every row =
  cp count; ConPTY model). Their Linux shaping-cluster behavior does
  not apply on this platform.

## 4. Artifacts
Drivers (tracked): `tools/shaped_probe.py`, `tools/proof_column_probe.ps1`,
`tools/run_proof_terminals.ps1` (installs Proof family, patches WT
settings / .wezterm.lua / alacritty.toml with backup+restore). Raw
results and captures (gitignored): `build/proof/` — `wt.txt`
`wezterm.txt` `alacritty.txt`, `cap_proof_wt.png`
`cap_proof_wezterm.png` `cap_proof_alacritty.png`.
